#include<iostream>
#include<map>
#include<string>
using namespace std;

map<int,string> unit;

void checkBase(int base){
	if(unit.size() < base){
		while(unit.size() == base){
			unit[unit.size()]
		}
	}
}

void changeBase(int n, int base){
	string str = "";
	while(n != 0){
		if(n > base){
			str.insert(0,)
		}
	}		
}

int main(){
	int n=31;
	int base = 16;
	checkMap(base);
	changeBase(n, base);
	return 0;
}
